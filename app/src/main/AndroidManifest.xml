<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gull_bemat.esiea.org" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="gull_bemat.esiea.org.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="gull_bemat.esiea.org.SecondeActivity"
            android:label="@string/title_activity_seconde" >
        </activity>

        <service
            android:name="gull_bemat.esiea.org.GetBiersServices"
            android:exported="false" >
        </service>
    </application>

</manifest>
